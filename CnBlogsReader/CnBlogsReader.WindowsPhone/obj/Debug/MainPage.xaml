<Page
    x:Class="CnBlogsReader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CnBlogsReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:localUserControls="using:CnBlogsReader.WindowsPhoneControls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Page.Resources>
        
    </Page.Resources>
    <Grid>
        <Hub x:ConnectionId='1' x:Name="HomeHub"                                                       >
            <HubSection x:Uid="Blogs" >
                <DataTemplate>
                    <ScrollViewer x:ConnectionId='2'                                        >
                        <localUserControls:BlogsListControl x:Name="bloglist"  BlogListSource="{Binding BlogListDataContent}" />
                    </ScrollViewer>
                    <!--<ListView x:Name="feedListView" ItemsSource="{Binding BlogListDataContent}" Grid.Row="1" FontSize="20" ScrollViewer.IsVerticalRailEnabled="True" ContainerContentChanging="feedListView_ContainerContentChanging">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid VerticalAlignment="Top" Margin="10,0,0,0" Height="80">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" >                                       
                                        <Image Source="ms-appx:///Assets/Logo.100.Blue.png" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="5">
                                        <TextBlock Text="{Binding ID}"  TextWrapping="Wrap" FontSize="15" Visibility="Collapsed"/>
                                        <TextBlock Text="{Binding Title}"  TextWrapping="Wrap" FontSize="15"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>-->
                </DataTemplate>
            </HubSection>
            <HubSection x:Uid="News">
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="新闻" FontSize="28"/>
                        <Button Content="anniu"/>
                    </StackPanel>
                </DataTemplate>
            </HubSection>
            <HubSection x:Uid="News">
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="推荐博文" FontSize="28"/>
                        <Button Content="anniu"/>
                    </StackPanel>
                </DataTemplate>
            </HubSection>
        </Hub>
        
    </Grid>
</Page>

