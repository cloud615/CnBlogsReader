<Page
    x:Class="CnBlogsReader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CnBlogsReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    
    <Page.Resources>
        
    </Page.Resources>
    <Grid>
        <Hub x:ConnectionId='1' x:Name="HomeHub"                                                       >
            <HubSection x:Uid="Blogs">
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="博文" FontSize="28"/>
                        <ListView x:Name="feedListView" ItemsSource="{Binding MainPageDataContent}" Grid.Row="1" FontSize="20">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel  VerticalAlignment="Top" Margin="10,0,0,0">
                                        <TextBlock Text="{Binding ID}"  TextWrapping="NoWrap" FontSize="15"/>
                                        <TextBlock Text="{Binding Title}"  TextWrapping="NoWrap" FontSize="15"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>                    
                </DataTemplate>
            </HubSection>
            <HubSection x:Uid="News">
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="新闻" FontSize="28"/>
                        <Button Content="anniu"/>
                    </StackPanel>
                </DataTemplate>
            </HubSection>
            <HubSection x:Uid="News">
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="推荐博文" FontSize="28"/>
                        <Button Content="anniu"/>
                    </StackPanel>
                </DataTemplate>
            </HubSection>
        </Hub>
        
    </Grid>
</Page>

