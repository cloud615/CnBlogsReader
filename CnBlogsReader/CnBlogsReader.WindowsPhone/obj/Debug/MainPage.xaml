<Page
    x:Class="CnBlogsReader.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CnBlogsReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Button x:Name="TestGet"  Content="发送Get请求" ></Button>
        </StackPanel>
        <ListView x:Name="feedListView" ItemsSource="{Binding MainPageDataContent}" Grid.Row="1" Header="列表" FontSize="20">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel  VerticalAlignment="Top" Margin="10,0,0,0">
                        <TextBlock Text="{Binding ID}"  TextWrapping="NoWrap" FontSize="15"/>
                        <TextBlock Text="{Binding Title}"  TextWrapping="NoWrap" FontSize="15"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

